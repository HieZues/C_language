#include <stdio.h>

int main (){
  int i, n, s = 0;
  printf ("Nhap n: ");
  scanf ("%d", &n);

  for (i= 1; i <= n; i++) {
    if ( i % 2 == 0){
        continue;
    }
    s += i;
  }
  printf ("Tong cac so le tu 1 den N la: %d", s);
  return 0;
}
